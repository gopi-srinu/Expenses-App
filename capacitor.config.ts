import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'expense.tracker.application',
  appName: 'expense-tracker-application',
  webDir: 'www'
};

export default config;
